# Michel Eirene I. Catienza, IT101 M3 FINAL PROJECT
# Game start menu

#=====#=====#=====#=====#=====#=====#=====#=====#=====#

# MODULES
"""import pygame
import pygame_menu

def StartMenu():
    self.state = GameState.MENU
    screen.fill (0, 0, 0)
    gameTitle = pygame.font.Font('NanoSquare.ttf', 30)
    gameTitleOut = pygame.font.Font('NanoSquareOutlin.ttf', 30)

    menuOpen = pygame.display.set_mode((450, 750))

    def StartMenu():
        self.state = GameState.MENU
        screen.fill(0, 0, 0)
        gameTitle = pygame.font.Font('NanoSquare.ttf', 30)
        gameTitleOut = pygame.font.Font('NanoSquareOutlin.ttf', 30)

        menuOpen = pygame.display.set_mode((450, 750))
        screen.blit(title, (screen_width / 2 - title.get_width() / 2, screen_height / 2 - title.get_height() / 2))
        screen.blit(start_button, (
        screen_width / 2 - start_button.get_width() / 2, screen_height / 2 + start_button.get_height() / 2))

        pygame.display.update()"""